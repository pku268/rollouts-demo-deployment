---
apiVersion: v1
kind: Service
metadata:
  name: service-canary
  labels:
    app: service-canary
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: rollouts-demo
